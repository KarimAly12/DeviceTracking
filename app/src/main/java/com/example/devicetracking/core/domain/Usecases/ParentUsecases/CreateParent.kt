package com.example.devicetracking.core.domain.Usecases.ParentUsecases

import androidx.compose.runtime.MutableState
import com.example.devicetracking.core.domain.model.ParentObject
import com.example.devicetracking.core.domain.repository.ParentRepository


class CreateParent(private val parentRepository: ParentRepository) {

//    suspend operator fun invoke(parent: ParentObject, password:String, isCreateProfile: MutableState<Boolean>):Boolean{
//
//        return parentRepository.createParent(parent, password, isCreateProfile)
//    }
}